"""Voice. Brief §11, plan §4 Stage 8.

Nothing here generates text. `tests/test_no_generation.py` walks this
package's AST and fails the build if it finds an import of anything that
could write a sentence for us — that test is the actual enforcement; this
docstring is just the summary of what it enforces.
"""
