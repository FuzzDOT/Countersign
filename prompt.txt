Read docs/STATE.md for where the build stands, then work through the
remaining stages of docs/03-BACKEND-PLAN.md §4 in order, following the
procedure in .claude/commands/next-stage.md for each one.

This is an unattended run — I am not available to answer questions. When you
hit a decision, an ambiguity, or a missing credential (NEMOTRON_API_KEY,
ELEVENLABS_API_KEY are both unset), make the most reasonable call consistent
with the plan and implement the real code path behind the existing MOCK_MODE /
classical-degrade / fallback design rather than skipping the stage. Record the
decision in docs/STATE.md. Do not stop to ask — there is no one to answer.

After each stage: run `make test-be`. If it's green, `git add -A && git
commit -m "stage N: <summary>"`. If you can't get it green after a reasonable
number of attempts, commit what you have anyway with a note in STATE.md about
what's failing, and move to the next stage rather than getting stuck on one.

Update docs/STATE.md after every stage — landed, next, blockers — so the
state on disk is accurate even if this run gets cut off mid-stage.

Keep going stage after stage until the plan is complete, or you're stopped by
the turn or budget limit.